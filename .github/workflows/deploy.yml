name: Deploy

on:
    push:
        branches:
            - work-flows

jobs:
    deploy:
        runs-on: self-hosted
        timeout-minutes: 5
        steps:
            - name: Checkout
              uses: actions/checkout@v3

            - name: Add Dir
              shell: bash
              run: |
                  mkdir RunnerWay
